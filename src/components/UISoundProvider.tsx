'use client';

import React from 'react';

export default function UISoundProvider({ children }: { children: React.ReactNode }) {
  // Click sounds removed completely as requested
  return <>{children}</>;
}
